# 酸橙味的信 · 安卓化任务书（2026-08-17 深夜定稿）

> 给下一个对话的 AI：本任务书自足。鸿蒙版项目背景/坑表读 Claude 记忆（project-harmonyos-pet）。

## 任务：安卓化——实现全部鸿蒙版功能

用户定调（原话）："在新对话中，我们将开始安卓化，实现全部鸿蒙版的功能"。鸿蒙版功能已冻结（不再加新功能），作为安卓版的**功能规格书与验收基准**。

### 功能全清单（安卓版须全部复刻）

1. **桌宠本体**：三模型（基建 build/战斗背 back/战斗正 front）Spine 3.8 骨骼动画、60fps、单击互动/双击菜单/长按拖动转向、点击语音（BASE_TALK/戳一下/战斗选中/作战中）、模式切换语音（部署/任命助理）、闲置语音、启动问候、字幕气泡（信使语义：寄信给「X」…/收到「X」的来信；安洁莉娜直聊=气泡流式输出内容）
2. **聊天终端**：侧边栏（会话列表/项目文件夹/主题/齿轮设置/四角星智能体入口）、Hero 空态、消息列表（Markdown 渲染）、输入卡片（项目胶囊/智能体小圆点/放大镜搜索/发送/停止■）、话题标题点击改名
3. **对话树**：RoundNode+path 模型、重新生成/编辑并重发开分支、分支切换、锚点删除警告、单条删除（deleteMessageSide）
4. **导航轨**：一轮一横条、正态分布伸缩、四行浮窗、黄色可见高亮（onVisibleAreaChange 追踪+窗口居中）、分支圆点/数字标记、条数上限、点击跳转
5. **项目**：文件夹分组、项目胶囊、移动到项目、无项目分类、展开状态持久化
6. **搜索**：输入框放大镜 → 当前会话活跃分支内搜索 → 发送者标识 → 点击定位
7. **智能体**：自定义智能体（名称+提示词）CRUD、默认智能体=安洁莉娜（Skill 注入锁定只读）、陌生的友人（'~raw' 无提示词直连）、对话参数（每对话上下文条数 Session.context_size + 人设查看/修改）、会话绑定 agent_aid（创建时烘焙）
8. **三个彩蛋**（额外提示词恰为 Key 才触发，Key 不进上下文，仅默认智能体生效；**隐秘性定案：实际对话用彩蛋 Skill，前端对话参数/人设展示永远显示安洁莉娜本体 Skill——只有聊起来才知道换了人**）：
   - 「你是普瑞赛斯」→ 普瑞赛斯 Skill（夺舍）
   - 「你是洁尔佩塔」→ 洁尔佩塔 Skill（再旅者）
   - 「酸橙味的信」→ 安洁莉娜本体 Skill（与过去的她对话）
   - Skill 文件在 rawfile/skill/（angelina_bundle.md / priestess_bundle.md / jieerpeita_bundle.md / angelina_past_bundle.md）
9. **陪伴模式**：摄像头实时背景 + 桌宠悬浮（CameraX 对应）、权限运行时申请、全屏沉浸、cover 裁剪
10. **设置**：API Key/地址/模型/测试连接/称呼/上下文/时间感知/额外提示词/碎碎念频率/语音开关语言/双主题
11. **自适应**：横竖屏（尊重系统旋转锁）、手机/平板差异化（手机桌宠 0.6、平板 0.9；输入框高度矩阵；手机竖屏侧边栏 140、隐藏导航轨）
12. **杂项**：全屏沉浸、应用名「酸橙味的信」、头像图标（D:\Angelina-pet-芋圆-代码重写\assets\avatar.png）、复制消息、自动跟随滚动（置底跟随，Chatbox 式）、停止生成、UTF-8 流式解码（stream 模式）

### 安卓化关键决策（与用户一起定）

1. **框架选型**：原生 Kotlin+Compose / Flutter / 其他——调研对比后用户拍板
2. **渲染**：Spine 运行时（Kotlin 重写骨骼计算 + GLES，或成熟 Android Spine 运行时）
3. **SSE 流式**：OkHttp SSE（对标鸿蒙铁律：普通请求会聚合缓冲，必须流式接口）
4. **相机**：CameraX 预览背景
5. **数据**：chat_history.json（对话树/项目/智能体/会话绑定）格式直接复用，可互通
6. **合规**：相机权限运行时申请、名称/图标沿用

### 数据安全三道闸（安卓版第一天就必须带上，2026-08-19 事故教训）

鸿蒙版发生过手机闪退+旧对话全部丢失（文件被覆写成空结构，不可恢复）。根因：①自动跟随滚动在 @Watch 回调里直接 scrollEdge（渲染阶段改滚动状态→崩溃）；②loadHistory 解析失败返回空结构→下一次保存覆写真实文件。**安卓版硬性要求**：
1. 保存必须原子写（tmp+rename）
2. 每次保存前自动备份上一版到 .bak
3. 加载失败先备份原文件再返回空结构
4. UI 回调（监听器/@Watch 等价物）里改滚动/布局状态，一律延迟出当前帧 + try/catch
5. **可见性回调陷阱（2026-08-19 事故教训）**：onVisibleAreaChange 等回调的闭包会跨越列表重建存活——列表被清空/替换后，旧回调仍会带着**旧下标**触发；回调内访问数组元素必须先验下标（`idx < arr.length`），否则 `arr[i].xxx` 访问 undefined → TypeError → 进程被杀。鸿蒙版曾因此"所有改数据操作闪退"（新对话/删话题/删消息/新建项目全中招），安卓版任何 RecyclerView/LazyColumn 的可见性/滚动回调同理

### 鸿蒙版环境速查（维护用）

- 构建/装包/双设备/备份命令见记忆或上一版 HANDOFF 的对应章节
- 待清理：PetPage.init 里的 `hilog PetDev deviceType` 诊断行

### 用户偏好

提问用文字不用弹窗；视觉保真敏感（比例颜色须精确）；成本敏感（上下文要节制）；V1 状态管理坑表见记忆。
